#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: 0(Success)
 */
int main(void)
{
	char f, m, l;
	int size;

	scanf("%c" "%c" "%c" "%d", &f, &m, &l, &size);
	printf("My initials are %c%c%c. They are %d in number\n", f, m, l, size);
	return (0);
}
