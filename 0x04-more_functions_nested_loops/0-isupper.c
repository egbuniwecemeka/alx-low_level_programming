/**
 * isupper - checks for uppercase character
 *
 * Return: 1 for uppercase, zero for others
 */

int _isupper(int c)
{
	int c;
	int upper;

	if (c >= 65 && c <= 90)
	{
		upper = 1;
	}
	else
	{
		upper = 0;
	}
	return (0)
}
